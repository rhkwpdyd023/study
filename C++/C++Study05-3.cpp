#include <iostream>
using namespace std;

int main() {

    int num1 = 3;
    int num2 = 7;

    cout << "!= 연산자에 의한 결괏값은 " << (num1 != num2) << "입니다." << endl;
    cout << ">= 연사자에 의한 결괏값은 " << (num1 >= num2) << "입니다." ;
    return 0;
    
}